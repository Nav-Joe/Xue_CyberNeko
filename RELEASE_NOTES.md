# Release Notes

## V0.6.1

### 屏幕偷窥：修复多轮后卡死

- **原因：** 陪玩旁白虽为串行模式，仍会一次向 TTS 侧车预取多路合成；句段多时队头请求卡住，整轮旁白无法结束，调度也被拖死。
- **修复：** 陪玩改为逐句提交合成，并为单句增加超时跳过；关桌宠时不再因已销毁窗口崩溃。
- **聊天 TTS：** 行为不变（仍沿用原有预取与并行设置）。

## V0.6.0

### 屏幕偷窥（里程碑 6）

打游戏时，雪澜可以偶尔「看一眼」你的画面，再用短旁白 + 对话 TTS 吐槽几句。旁白**不会**写进聊天记录。

- **默认关闭。** 关着时不截屏、不扫进程、不旁白。
- **首版只认 Steam 游戏：** 本机 Steam 库里的游戏在跑时才会进入陪玩；浏览器 / IDE 等后置。
- **看屏方式：** 按你设定的间隔截取缩略图（内存处理、**原图不落盘**），压成短文字摘要后再说话；摘要会发往你配置的**视觉 API**（与聊天 API 分开）。
- **使用条件：** 需先开对话 TTS；视觉 Base URL / 模型 / Key 配齐后才会真正观察。
- **和聊天互斥：** 在玩陪玩会话中发送聊天会被系统弹窗拦住；关掉游戏或退会话后恢复。
- **记忆（可选）：** 记忆总闸开启时，旁白与屏幕摘要可记入本机；退陪玩会话后整理进记忆库，记忆空间里能看到陪玩总结。
- **设置入口：** 桌宠右键 → 设置 →「屏幕偷窥」

### 隐私提醒

对话、记忆、好感、API Key、屏幕摘要等仍只应留在本机。勿把 `%APPDATA%/xue-cyber-neko/` 下的配置与数据库贴到 Issue 或提交进仓库。屏幕偷窥相关文件见 README「隐私与本地数据」。
