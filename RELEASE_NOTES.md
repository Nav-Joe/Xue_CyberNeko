# Release Notes

## V0.6.2

### 屏幕偷窥：旁白 TTS 可选 CPU / GPU

- 设置页新增 Apple 风格分段切换：**CPU**（默认，不占游戏显卡）或 **GPU**（旁白更快，适合轻量游戏或显卡有余量时）。
- 配置写入 `screen-companion-config.json`，切换后立即生效。

## V0.6.1

### 屏幕偷窥：修复多轮后卡死

- **原因：** 句段多时队头请求卡住，整轮旁白无法结束，调度也被拖死。
- **修复：** 改为逐句提交合成，并为单句增加超时跳过。
- **聊天 TTS：** 行为不变（仍沿用原有预取与并行设置）。

